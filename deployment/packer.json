{
  "variables": {
    "aws_access_key": "{{env `aws_access_key`}}",
    "aws_secret_key": "{{env `aws_secret_key`}}",
    "cf_db_host": "",
    "rsyslog_port": "{{env `rsyslog_port`}}",
    "rsyslog_host": "{{env `rsyslog_host`}}"
  },
  "builders": [{
    "type": "amazon-ebs",
    "access_key": "{{user `aws_access_key`}}",
    "secret_key": "{{user `aws_secret_key`}}",
    "region": "us-east-1",
    "source_ami": "ami-36b9705e",
    "instance_type": "m1.small",
    "ssh_username": "ubuntu",
    "ami_name": "congress-forms {{timestamp}}"
  }],  
  "provisioners": [{
    "type": "shell",
    "inline": [
      "sudo mkdir -m 777 /vagrant/"
    ]
  },
  {
	"type": "file",
	"source": "../",
	"destination": "/vagrant"
  },
  {
    "type": "shell",
	"execute_command":	"echo '' | {{ .Vars }} sudo -E -S sh {{ .Path }} ubuntu {{user `cf_db_host`}}",
    "scripts": [
      "../setup_dev.sh",
      "service_setup.sh"
    ]
  }
  ]
}
